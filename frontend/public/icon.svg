<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Background -->
  <rect width="512" height="512" fill="#4F46E5" rx="80"/>

  <!-- Barbell -->
  <g fill="white">
    <!-- Left collar (small separator) -->
    <rect x="110" y="240" width="15" height="32"/>

    <!-- Left weight plates (largest to smallest) -->
    <rect x="75" y="165" width="35" height="182" rx="10"/>
    <rect x="60" y="175" width="15" height="162" rx="8"/>
    <rect x="50" y="190" width="10" height="132" rx="6"/>

    <!-- Center bar (straight, no rounding) -->
    <rect x="125" y="242" width="262" height="28"/>

    <!-- Right collar (small separator) -->
    <rect x="387" y="240" width="15" height="32"/>

    <!-- Right weight plates (largest to smallest) -->
    <rect x="402" y="165" width="35" height="182" rx="10"/>
    <rect x="437" y="175" width="15" height="162" rx="8"/>
    <rect x="452" y="190" width="10" height="132" rx="6"/>
  </g>
</svg>
